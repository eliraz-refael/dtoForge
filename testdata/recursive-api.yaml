openapi: 3.0.0
info:
  title: Recursive Schema Test API
  version: 1.0.0
components:
  schemas:
    TreeNode:
      type: object
      required:
        - name
      properties:
        name:
          type: string
        children:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/TreeNode'

    FacetFieldDateInterval:
      type: object
      properties:
        unit:
          type: string

    FacetSearchFieldRequest:
      type: object
      properties:
        dateInterval:
          oneOf:
            - $ref: '#/components/schemas/FacetFieldDateInterval'
            - type: 'null'
        fieldPath:
          type: string
          nullable: true
        subFieldFacets:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/FacetSearchFieldRequest'

    SimpleDTO:
      type: object
      required:
        - id
      properties:
        id:
          type: string
        label:
          type: string
